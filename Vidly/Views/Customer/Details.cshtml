@model Vidly.Models.Customer 

@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>
<ul>
    <li>Membership Type: @Model.MemberShipType.Name</li>
    @if (Model.BirthDate != null)
    {
        <li>Birthday: @Html.DisplayFor(x => x.BirthDate)</li>
    }
</ul>

